@model AkademisyenProfil.Models.Oduller
@{
    ViewData["Title"] = "odulekle";
    Layout = "~/Views/Shared/adminlayout.cshtml";
}

<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-header">
                <strong>ÖDÜL EKLE</strong>
            </div>
            <div class="card-body card-block">
                <form asp-controller="Admin" asp-action="odulekle" method="post" enctype="multipart/form-data" class="form-horizontal">
                    <label><strong>YENİ ÖDÜL</strong></label>
                    <input type="text" class="form-control" name="odulad" />
                    <br />
                    <label><strong>AKADEMİSYEN</strong></label>
                    @Html.DropDownListFor(m => m.Akademisyenler.akano, (List<SelectListItem>)ViewBag.dgr, new { @class = "form-control" })
                    <br />
                    <button class="btn btn-primary btn-sm">
                        <i class="fa fa-dot-circle-o"></i> Ödül Ekle
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

