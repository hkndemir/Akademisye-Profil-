@model AkademisyenProfil.Models.Projeler
@{
    ViewData["Title"] = "projeekle";
    Layout = "~/Views/Shared/adminlayout.cshtml";
}

<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-header">
                <strong>PROJE EKLE</strong>
            </div>
            <div class="card-body card-block">
                <form asp-controller="Admin" asp-action="projeekle" method="post" enctype="multipart/form-data" class="form-horizontal">
                    <label><strong>YENİ PROJE</strong></label>
                    <input type="text" class="form-control" name="projead" />
                    <br />
                    <label><strong>AKADEMİSYEN</strong></label>
                    @Html.DropDownListFor(m => m.Akademisyenler.akano, (List<SelectListItem>)ViewBag.dgr, new { @class = "form-control" })
                    <br />
                    <br />
                    <button class="btn btn-primary btn-sm">
                        <i class="fa fa-dot-circle-o"></i> Proje Ekle
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

