@model AkademisyenProfil.Models.Hakemlikler
@{
    ViewData["Title"] = "hakemlikekle";
    Layout = "~/Views/Shared/adminlayout.cshtml";
}

<div class="row">
    <div class="col-lg-12">
        <div class="card">
            <div class="card-header">
                <strong>HAKEMLİK EKLE</strong>
            </div>
            <div class="card-body card-block">
                <form asp-controller="Admin" asp-action="hakemlikekle" method="post" enctype="multipart/form-data" class="form-horizontal">
                    <label><strong>YENİ HAKEMLİK</strong></label>
                    <input type="text" class="form-control" name="hakemad" />
                    <br />
                    <label><strong>AKADEMİSYEN</strong></label>
                    @Html.DropDownListFor(m => m.Akademisyenler.akano, (List<SelectListItem>)ViewBag.dgr, new { @class = "form-control" })
                    <br />
                    <button class="btn btn-primary btn-sm">
                        <i class="fa fa-dot-circle-o"></i> Hakemlik Ekle
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>

